{
  "functions": {
    "api/**/*.py": {
      // ✅ 이 부분을 삭제하거나, functions 객체 전체를 삭제합니다.
      // "includeFiles": "data/predict_db/**" 
    }
  },
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build"
    },
    {
      "src": "api/**/*.py",
      "use": "@vercel/python"
    }
  ]
}